# Copy this file to .env for persistent local settings.
# .env is loaded automatically by serve-vault.py and ignored by Git.
# Never commit a real API key.
OPENSEA_API_KEY=

# Optional server settings. HOST is intentionally restricted to loopback.
PORT=7777
HOST=127.0.0.1
